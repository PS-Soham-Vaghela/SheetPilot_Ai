# API layer
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32

# Auth — JWT + password hashing (web dashboard)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# MongoDB (sync driver — thread-safe connection pooling)
pymongo==4.12.1
dnspython>=2.6.0

# Web scraping for server-side URL analyze
beautifulsoup4==4.12.3
lxml>=5.0.0

# MCP SDK (1.29.0 is last stable 1.x; compatible with fastapi 0.141+)
mcp==1.29.0

# RAG pipeline
sentence-transformers==3.0.1
faiss-cpu==1.9.0

# LLM — Groq API (cloud inference, ~1-3s latency)
groq==1.6.0

# Excel read/write
openpyxl==3.1.4

# Data validation (fastapi 0.141 requires pydantic>=2.9, mcp 1.29 requires pydantic>=2.8)
pydantic==2.11.7
pydantic-settings==2.14.2

# Utilities
python-dotenv==1.2.2
httpx==0.27.2    # groq 1.6.0 uses httpx's old `proxies=` kwarg removed in 0.28
websockets==13.1
